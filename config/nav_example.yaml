# Moved: nav stack (MPPI / Dynamics / Cost / Planner / …) is under
# ``nav:`` in ``hound_core/config/SSoT.yaml``. Dora nodes read HOUND_NAV_CONFIG
# embedded from that section at launch — this file is unused.
